[mcp_servers.codex-coordinator]
url = "https://codex-coordinator.cclib.workers.dev/mcp"
auth = "oauth"
required = true
tool_timeout_sec = 75
default_tools_approval_mode = "approve"
http_headers = { "X-Codex-Coordinator-Project" = "worldcons" }

# register_session returns a unique <project>:<role-or-agent>:<instance> value.
# Include that exact session_id in every later mutation tool call from this task.
