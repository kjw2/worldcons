{
  "name": "@worldcons/chatgpt-plugin-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types worker-configuration.d.ts",
    "check": "pnpm types && tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.22.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "4.114.0"
  }
}
